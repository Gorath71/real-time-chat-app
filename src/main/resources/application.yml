spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/real-time-chat-app-db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: real-time-chat-app

  security:
    user:
      name: user
      password: P@ssword123!